// Copyright 2026 icefelix.com. BSD-3-Clause.
// Smoke widget tests live in integration_test/ — they need a real window.
// This file is intentionally a no-op so `flutter test` from example/ doesn't
// fail on the default scaffolded widget_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('example test placeholder', () => expect(1 + 1, 2));
}
